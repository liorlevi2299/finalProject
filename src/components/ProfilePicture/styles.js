import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 4,
    borderRadius: 43,
    borderWidth: 3,
    borderColor: '#f75e5f',
  },
  image: {
    borderRadius: 40,
    borderWidth: 3,
    borderColor: 'white',
  },
});

export default styles;
